[project]
name = "job-search"
version = "1.0.0"
description = "Multi-source job search pipeline for data/analytics roles"
requires-python = ">=3.10"

[project.optional-dependencies]
workday = ["playwright"]
